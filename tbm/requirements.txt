# TorBox Manager EchoStorm Edition
# Python dependencies
#
# Install via launch.bat (automatic on first run), or manually:
#   venv\Scripts\pip install -r requirements.txt
#
# Pinned to minimum known-good versions.
# PyQt6 6.5+ is required for stable QThreadPool behaviour on Windows.
# requests 2.28+ covers the streaming and exception handling used in worker.py.

PyQt6>=6.5.0
requests>=2.28.0

# Optional — archive extraction support
# py7zr handles .7z files (pure Python, no external binary needed)
# rarfile handles .rar files (requires unrar.exe or WinRAR in PATH)
py7zr>=0.20.0
rarfile>=4.0
